<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.zhaw.engineering.tbdappname">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App"
        android:usesCleartextTraffic="true">
        <activity android:name=".AddOrEditRadioStationActivity"></activity>
        <activity android:name=".CurrentlyPlayingActivity" />
        <activity android:name=".AddOrEditPlaylistActivity" />
        <activity android:name=".PlaylistListActivity" />
        <activity
            android:name=".SongListActivity"
            android:label="@string/title_activity_song_list"
            android:theme="@style/Theme.App.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DirectorySelectionActivity" />
        <activity android:name=".PlsFileSelectionActivity" />
        <activity android:name=".RadioStationListActivity" />

        <service
            android:name=".services.files.AudioFileScanner"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />
        <service
            android:name=".services.audio.AudioService"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>